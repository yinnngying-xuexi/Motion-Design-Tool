# DataMotion 动效编辑器：已确认产品规则

> 更新日期：2026-08-13
> 本文件用于在上下文压缩或新会话中恢复项目共识。这里只记录已经由用户确认、且当前仍然有效的产品与实现规则。若旧文档、旧截图或历史代码与本文件冲突，以本文件和当前代码为准。

## 1. 协作规则

- 始终使用中文与用户交流。
- 在用户明确要求讨论、确认方案时，先讨论，不直接修改代码。
- 修改时保持请求范围，不顺带调整其他页面、布局或动效。
- 不重新搭建项目，继续基于当前 Vue 3、TypeScript、Vite、Element Plus 项目修改。
- 不主动提交 Git、推送 GitHub 或部署；只有用户明确要求时才执行。
- 预览、复制代码、导出 HTML 必须使用同一套参数与动效逻辑，不能出现编辑器有效、导出失效的情况。

### 1.1 全局开发规范

- 保持代码结构清晰且尽量精简，优先复用项目现有组件、工具函数和公共动效逻辑。
- 动效优先使用 CSS / SVG 原生能力实现；非必要不使用 JavaScript，不新增不必要的第三方依赖。
- 旋转、流光、呼吸、扫光、描边、路径运动等相同基础动效能力必须统一复用，不得为不同模板重复开发。
- 参数集中管理，避免重复写死和过度封装。
- 基础动效属于能力层，装饰组件属于组合层；装饰组件内部各图层调用已有基础动效能力，不重新实现同类动画。
- 新增功能前先检查项目中是否已有可复用能力；能复用则直接复用，只有确实缺失时才新增公共能力。
- 导出代码必须与编辑器逻辑解耦，只保留当前动效运行所需的最小 HTML / CSS / JavaScript，确保前端可直接使用。
- 删除无用、重复、临时和测试代码，但不得为了减少代码量影响可读性和维护性。
- 不修改本次需求范围之外的模块；如确实必须影响其他模块，先向用户说明原因和影响范围并获得确认。
- 后续所有新增、修改、重构、动效组件、装饰组件、SVG 处理、复制代码和导出 HTML 等开发任务默认遵守本节规则，无需用户重复说明。

## 2. 产品定位与数据原则

- 产品是面向大屏可视化设计工作的本地动效编辑器，用于浏览案例、导入素材、调整动效并交付 HTML/CSS。
- 产品采用本地优先模式，不增加账号、云同步、后台数据库或多人实时协作。
- 用户及其同事分别在各自电脑、本地浏览器中使用；不同电脑的数据不会自动同步，也不会互相冲突。
- 浏览器本地地址（如 `127.0.0.1:5173`）只用于开发预览，关闭开发服务或重启电脑后需要重新启动。
- Netlify 是对外公开版本；本地修改不会自动进入 Netlify，必须重新构建和部署。
- “项目交付”目前从导航和页面流程中隐藏。原实现文件可以保留，但未经用户再次确认，不得重新接回界面。

## 3. 整体视觉规则

- 整体使用黑色背景、白色文字和灰色层级，风格偏高级设计工具，不做传统后台管理系统，也不做夸张科技大屏界面。
- 禁止恢复黄色主色。
- 品牌蓝统一使用 `#0070F3`，只用于主要操作、动效主体和必要的交互强调。
- 减少边框、阴影、分割线和大面积高亮，依靠留白、字号、字重和灰度建立层级。
- 首页与所有编辑内页必须使用同一套品牌、一级导航、暗色背景和蓝色操作规范。
- “保存到我的动效”和“复制代码”等主要按钮使用品牌蓝；普通操作使用低对比中性色。
- 中间工作区的“导入 SVG、导出 HTML、复制代码”按钮使用小圆角，当前标准为 `4px`，不使用胶囊按钮。
- 参数数值框统一宽度为 `60px`。
- 参数滑杆手柄统一为 `16 × 16px`。
- 数字输入框不显示突兀的白色下调框；步进箭头和分隔线保持低透明度。

## 4. 统一像素格画布

- 所有动效预览画布复用 `dm-motion-canvas`。
- 网格尺寸为 `18 × 18px`。
- 网格线为 `1px` 低透明白线。
- 画布底色为 `#111111`。
- 像素格覆盖：
  - 首页动效案例。
  - 基础动效列表缩略图和主预览。
  - 装饰组件列表缩略图和主预览。
  - 自定义素材预览。
  - “我的动效”预览区域。
- 首页卡片、列表缩略图和编辑器主预览必须渲染同一个真实动效，不能使用模糊截图或不对应的示意图。
- 像素格只属于编辑器预览视觉，导出的独立 HTML 不携带网格，使用干净的黑色背景。

## 5. 页面结构

### 5.1 全局框架

- 顶部品牌为“DM 动效编辑器 V2.0”，点击品牌返回动效预设首页。
- 首页不显示编辑器搜索和保存按钮。
- 进入基础动效或装饰组件编辑页后，顶部显示搜索框和蓝色“保存到我的动效”。
- 顶部不再放重复的“导出”按钮；导出入口位于中间工作区。
- 左侧一级导航宽度保持紧凑，当前标准为 `206px`。
- 一级导航固定为：
  - 动效预设
  - 装饰组件
  - 我的动效
  - 自定义素材
- 不显示“项目交付”。

### 5.2 动效预设首页

- 首页定位是动效案例展示平台，不是后台列表或参数配置页。
- 桌面端卡片四个一排，使用较大的真实预览区域和充分间距。
- 默认展示 10 个精选案例。
- 卡片展示动效名称、英文名称或类型、简短说明、时长和使用场景。
- 鼠标悬停时自动播放预览；键盘可访问状态保留预览能力。
- 鼠标单击一次必须直接进入对应编辑器，不能要求点击两次。
- 首页不放参数、项目配置、复杂筛选或大量分类按钮。
- 首页搜索是编辑器案例的全局搜索：
  - 覆盖全部基础动效和装饰组件。
  - 搜索中文名称、英文名称、说明和使用场景。
  - 不搜索“我的动效”和自定义 CSS 素材。
  - 基础动效结果进入基础动效编辑页。
  - 装饰组件结果进入装饰组件编辑页并选中对应效果。

### 5.3 基础动效编辑页

- 页面结构固定为：
  - 最左侧：与首页一致的一级导航。
  - 左侧二级栏：基础动效分类和动效列表，宽度约 `200px`。
  - 中间：当前动效标题、说明、视图切换、操作区和工作画布。
  - 右侧：当前动效专属参数。
- 当前动效标题字号为 `28px`。
- 中间只保留两个视图：
  - 动效预览
  - 代码展示
- 两个视图共用同一块内容区域，不能同时上下堆叠显示。
- 代码展示直接显示当前 HTML + CSS，不再显示 HTML、Vue、JSON 等二级格式标签。
- 中间操作顺序固定为：
  1. 导入 SVG
  2. 导出 HTML
  3. 复制代码
- 顶部蓝色“保存到我的动效”继续保留，不在中间区域重复保存按钮。

### 5.4 装饰组件编辑页

- 装饰组件编辑页与基础动效编辑页使用同一种页面结构和视觉规则。
- 左侧二级栏宽度约 `200px`，右侧参数栏约 `320px`。
- 中间同样使用“动效预览 / 代码展示”切换。
- 中间操作顺序同样为“导入 SVG、导出 HTML、复制代码”。
- 主标题只显示当前动效名称和说明，不重复显示板块名称标签。
- 装饰组件一级分类固定为：
  - 标题装饰。
  - 图表装饰。
  - 图标/点位。
  - 面板装饰。
  - loading。
- 当前已有内容归属为：
  - 图标/点位 → 图标底座：星环粒子底座。
  - 标题装饰 → 顶部标题：单边流光、双边流光、水波纹流光。
  - 标题装饰 → 小标题：扫光。
- 图表装饰和面板装饰当前只保留分类位置，不虚构或恢复旧组件。
- loading 当前提供六个跨项目通用系统案例：
  - 旋转圆环：低亮度底环与渐变高亮弧持续旋转，支持循环时长、方向、尺寸、线宽、高亮弧长、轨道色和主色。
  - 点序呼吸：`3–5` 个圆点依次放大和提亮，支持循环时长、数量、大小、间距、最低透明度和主色。
  - 线性流光：高亮光段沿低亮度轨道移动，支持无限加载与确定进度两种模式，以及时长、进度、轨道宽度、线宽、流光长度、圆角和颜色。
  - 图标脉冲：默认图标轻微呼吸并扩散柔和光环，支持导入外部 SVG 替换默认图标，并提供循环时长、尺寸、最小缩放、光环显隐、范围、强度和主色。
  - 科技环形：参考 CodePen “Futuristic Loader” 的双层同心光环和偏心高亮旋转方式，去掉原案例的计数、jQuery、外部字体与页面级样式；仅保留纯 HTML + CSS。中心默认显示 `Loading`，支持修改中心文字、旋转速度、内外环大小、内外环粗细、内外环发光强度和主色；中心文字保持静止，两个环同步旋转。
  - 不规则旋转环：参考 CodePen “Futuristic Loader” 的多层不规则环交错旋转结构，统一改为品牌蓝；使用三条内联 SVG 闭合路径分别进行正向、反向和轻微缩放旋转，并支持旋转速度、尺寸、线条宽度、发光强度和主色；不依赖 JavaScript、外部资源或全局样式。
- 六个 loading 均使用透明内容背景，编辑器像素格不得进入交付文件；预览、代码展示、复制代码和导出 HTML 使用同一套参数与原生 CSS / SVG 动效。
- loading 不默认开启粒子效果；粒子只作为装饰组件共用的可选附加效果。
- “星环粒子底座”直接使用用户确认的分层 SVG 作为系统预设素材，不再使用旧的程序绘制底座。
- 星环右侧“实际素材图层”显示素材的真实英文图层名、对应中文名称和眼睛开关；不显示 SVG 命名说明或底部“绑定角色”卡片。
- 星环预设 SVG 图层固定显示为 `background、outer-ring、inner-ring、center`；粒子不再伪装成 SVG 图层，而是装饰组件共用的独立附加效果。
- 所有装饰组件都提供一个可选“附加效果 / 粒子效果”区域；每个组件当前最多启用一套共用粒子系统。
- 粒子系统与 SVG 图层命名和映射解耦，导入的 SVG 不需要自带粒子图层；星环默认开启，顶部标题流光默认关闭。
- 粒子样式提供漂浮、闪烁和扩散，并统一支持数量、大小、强度、速度、分布宽高、水平/垂直位置、素材前后层级以及继承主色/自定义颜色。
- 粒子配置必须随当前装饰组件一起保存，并在预览、复制代码和导出 HTML 中保持一致；关闭粒子后不得输出粒子节点或粒子 CSS。
- 导出的粒子动效优先使用 CSS 原生动画，不携带编辑器配置 JSON、参数面板逻辑或第三方运行时。
- 后续装饰素材沿用“真实 SVG 图层即编辑图层”的规则：导入后保留 SVG 原始英文命名，界面不翻译、不重命名，内部自动关联对应动效。
- 分层 SVG 优先读取素材根分组下的直接子图层；已命名的 `g` 与直接矢量形状都可成为素材图层，子分组内部的“光、矢量”等细节不得被提升为顶层图层。
- 外环名称同时兼容 `outer-ring` 与 `out-ring`，界面始终保留 SVG 中的原始名称。
- `inner-ring` 的环形高亮轮转支持分段直接位于图层内，也支持 Figma 将多个分段统一包在一个子分组内；子分组中的独立形状按环形位置排序后依次高亮。
- 为获得稳定的环形高亮轮转，推荐在 `inner-ring` 内将可轮转部分命名为 `segment-01、segment-02…`，至少 8 段，推荐 12–16 段；各分段保持同级，或统一放入一个 `ring-segments` 子分组。
- `inner-ring` 中不参与轮转、仅作为低亮度完整轮廓的底环命名为 `ring-base`，不得使用 `segment` 前缀。
- 未分段的连续路径仍可导入；编辑器使用移动高亮遮罩作为兼容方案，不旋转已经做好的假透视几何，但精细度低于真实分段素材。
- 导入分层 SVG 时保留“图层识别与动效映射”弹窗，自动识别结果允许用户确认或手动调整；应用后右侧仍使用 SVG 原始英文图层名。
- 已删除的环形图标底座、菱形脉冲底座、扫描装饰、边框光效、水平线性流光和折线路径流光不得恢复。
- 顶部标题当前提供三个独立系统案例：
  - 单边流光：单条标题路径，素材原始尺寸为 `1920 × 96`，默认拖尾长度 `420px`、流光线宽 `2px`、发光强度 `14px`，三段颜色为浅绿 `#A7F3D0`、主绿 `#10B981`、深绿 `#064E3B`。
  - 双边流光：左右两条路径向中间聚拢，素材原始尺寸为 `1920 × 84`，默认三段颜色为浅蓝 `#7AB8FF`、主蓝 `#0070F3`、深蓝 `#003B82`。
  - 水波纹流光：只读取并保留 SVG 的 `background` 作为静态标题框，不使用素材自带的任何“光”图层；动效参照用户确认的 `huaban-5110384198.gif`，在标题框内部生成三层独立起伏的蓝青色半透明水流丝带，不增加黑色背景；后层为低对比蓝色薄雾，中层提供透明流体体积，前层带更亮、更细的青色波峰，并叠加低透明网点质感；所有效果裁切在标题框内；素材尺寸为 `1920 × 77`。
- 单边流光、双边流光支持导入外部 SVG 替换系统路径；水波纹流光导入时只读取 `background`，外部光层会被忽略，统一复用编辑器生成的液态光流逻辑，不要求提供流光路径或光效图层。
- 水波纹流光是无停顿的持续背景水流，默认使用 `5.4s` 流动周期、`68%` 光效强度和 `9px` 流动幅度；三层丝带使用不同周期和相位交叠流动，主体保持低对比并统一增加柔和模糊，不能同步平移或退化为单层扫光；右侧“形态设置”提供宽幅位置、左端宽度和右端宽度，宽幅位置可在整条标题框内自由移动，端点宽度 `100%` 表示该端与最宽处等宽，较小数值则形成自然收窄；默认宽幅位置为左侧 `14%`、左端宽度 `85%`、右端宽度 `25%`；首尾不得被写死为极细尖线；右侧不显示间隔或缓动曲线。
- 单边流光、双边流光的多路径 SVG 可通过 `flow-path-left`、`flow-path-right` 等具名路径分别设置流动方向和开始延迟。
- 三个顶部标题流光均提供“速度变化”，可选匀速、缓入、缓出、缓入缓出；预览和导出使用同一套速度曲线。
- 顶部标题流光在编辑器中按画布等比缩放完整展示，复制和导出时保持 SVG 原始尺寸。
- “扫光”以用户提供的 `410 × 40` 小标题 SVG 作为系统案例；标题框与圆形装饰保持静止，编辑器自动在素材内部生成从左向右移动并自然消失的柔和光效。
- “扫光”系统案例不保留原素材的 `guide-line` 图层，右侧实际素材图层、预览、复制代码和导出 HTML 均不显示或输出该图层。
- “扫光”导入外部 SVG 后继续自动生成移动光效，不要求用户在 Figma 中设计或命名“光”图层；若素材中存在名为 `光、light、sweep、sweep-light、移动光效` 的顶层光效图层，该图层在当前模板中忽略，避免与系统移动光效重复。
- “扫光”的右侧移动光效参数包括移动时长、循环间隔、速度变化、扫光形状、渐变起色、渐变终色、光效宽度、边缘模糊和光效强度；扫光形状支持弧形、椭圆和柔光带，默认参数为椭圆、渐变 `#4DC9FF → #0070F3`、宽度 `76px`、边缘模糊 `12px`、光效强度 `20%`；光效完全使用用户设置的两端渐变色，不混入固定白色；预览、复制代码和导出 HTML 使用同一套参数。
- 小标题 SVG 的其他实际素材图层保留原始图层名、眼睛显隐开关，并可逐层绑定已有基础动效；图层动效与系统自动移动光效相互独立。
- 小标题案例在编辑器中等比适配完整展示，复制和导出时保持 SVG 原始尺寸与图层结构。

### 5.5 我的动效

- “保存到我的动效”后，卡片至少保存并展示：
  - 动效名称。
  - 可独立运行的 HTML。
  - 动效预览 PNG。
- 保存位置是当前浏览器、当前站点的本地存储，键名为 `visual-motion-my-motions`。
- 相同动效再次保存时更新已有记录，不重复新增同 ID 记录。
- 已保存内容可以预览、下载和删除。
- 当前“单边流光、双边流光、水波纹流光”有明确的再次编辑入口。
- 基础动效和普通装饰组件暂不支持从保存记录恢复全部参数继续编辑。

### 5.6 自定义素材

- 自定义素材当前支持上传或粘贴变量化 CSS 模板。
- 建议模板使用 `.custom-asset-target` 作为预览选择器。
- 系统读取 CSS 自定义变量并映射到右侧参数控件。
- 保存后的素材显示在“自定义素材”页面左侧列表。
- 数据保存在当前浏览器本地，键名为 `visual-motion-custom-css-assets`。
- 自定义素材不会自动写入电脑文件夹，也不会上传服务器。
- 更换浏览器、端口、设备或清理站点数据后，不会自动同步或恢复。
- 自定义素材保留 HTML + CSS、CSS、JSON Config 的代码查看和导出能力。

## 6. 基础动效分类与内容

- 基础动效分类只保留：
  - 全部
  - 入场动效
  - 循环动效
  - 强调动效
  - 告警动效
- 不显示“大屏装饰”分类。
- “地图光点闪烁”已删除，不得恢复。
- 当前基础动效包括：
  - 入场：淡入、上滑进入、左滑进入、缩放进入。
  - 循环：呼吸、上下浮动、轻微闪烁、光效脉冲、慢速旋转。
  - 强调：放大提示、高亮发光、边框高亮、边框流光。
  - 告警：告警闪烁、脉冲扩散。

## 7. 参数面板通用规范

- 参数必须跟随当前动效动态显示，禁止所有动效共用一套通用参数全集。
- 当前动效不需要的参数不得出现。
- 参数按“基础参数”和“效果参数”组织；只展示实现当前动效所需字段。
- 循环动效默认无限循环，不展示通用“播放次数”和 CSS“播放方向”。
- “慢速旋转”的顺时针/逆时针是该动效专属效果参数，不属于通用播放方向。
- 参数面板不得擅自增加模糊、阴影、发光、颜色、位移、旋转等无关字段。
- 每个动效在当前编辑会话中独立记忆参数；切换后返回时保留本次会话调整值。
- 刷新页面后，基础动效参数从各自动效默认值开始。
- “重置”只恢复当前动效默认值，不影响其他动效。
- 滑块拖动时实时更新画面与数值；停止拖动后重新播放一次。
- 数值输入在失焦或按 Enter 后应用并重播。
- 参数重播可使用约 `150ms` 防抖，避免拖动时频繁重启动画。
- 切换动效后停止上一预览，避免多个循环动效同时运行。

## 8. 已确认的入场动效参数

### 8.1 淡入

- 基础参数：
  - 动效时长，默认 `0.8s`。
  - 开始延迟，默认 `0s`。
  - 缓动曲线，默认 `ease-out`。
- 效果参数：
  - 起始透明度，默认 `0`。
- 结束透明度固定为 `1`。
- 不增加无关的位移、缩放、旋转、阴影或发光参数。

### 8.2 上滑进入

- 基础参数：
  - 动效时长：默认 `0.7s`，范围 `0.2–2s`，步长 `0.05s`。
  - 开始延迟：默认 `0s`，范围 `0–2s`，步长 `0.05s`。
  - 缓动曲线：默认 `ease-out`。
- 效果参数：
  - 进入距离：默认 `36px`，范围 `0–160px`，步长 `4px`。
  - 起始透明度：默认 `0`，范围 `0–1`，步长 `0.05`。
- 运动逻辑：`translateY(进入距离) → translateY(0)`，结束透明度固定为 `1`。

### 8.3 左滑进入

- 基础参数与上滑进入一致，默认时长 `0.7s`。
- 效果参数：
  - 进入距离：默认 `36px`，范围 `0–160px`，步长 `4px`。
  - 起始透明度：默认 `0`，范围 `0–1`，步长 `0.05`。
- 运动方向是从最终位置右侧向左进入。
- 运动逻辑：`translateX(进入距离) → translateX(0)`，不能使用负向起始距离。

### 8.4 缩放进入

- 基础参数与上滑进入一致，默认时长 `0.65s`。
- 效果参数：
  - 起始缩放：默认 `0.85`，范围 `0.7–1`，步长 `0.01`。
  - 起始透明度：默认 `0`，范围 `0–1`，步长 `0.05`。
- 运动逻辑：`scale(起始缩放) → scale(1)`，结束透明度固定为 `1`。

## 9. 已确认的循环动效参数

### 9.1 呼吸

- 基础参数：
  - 循环时长：默认 `2.2s`，范围 `1–6s`，步长 `0.1s`。
  - 开始延迟：默认 `0s`，范围 `0–3s`，步长 `0.1s`。
  - 缓动曲线：默认 `ease-in-out`。
- 效果参数：
  - 最小缩放：默认 `0.96`，范围 `0.85–0.99`，步长 `0.01`。
- 最大缩放固定为 `1`。
- 动画逻辑：`scale(最小缩放) ↔ scale(1)`。
- 不显示透明度、发光或阴影参数。

### 9.2 上下浮动

- 基础参数：
  - 循环时长：默认 `2.4s`，范围 `1.2–6s`，步长 `0.1s`。
  - 开始延迟：默认 `0s`，范围 `0–3s`，步长 `0.1s`。
  - 缓动曲线：默认 `ease-in-out`。
- 效果参数：
  - 浮动距离：默认 `8px`，范围 `2–30px`，步长 `1px`。
- 动画逻辑：`translateY(0) → translateY(-浮动距离) → translateY(0)`。
- 不显示横向位移、透明度或旋转参数。

### 9.3 轻微闪烁

- 只表示透明度柔和循环变化，不加入发光颜色、光晕范围或闪烁频率。
- 基础参数：
  - 循环时长：默认 `1.8s`，范围 `0.8–5s`，步长 `0.1s`。
  - 开始延迟：默认 `0s`，范围 `0–3s`，步长 `0.1s`。
  - 缓动曲线：默认 `ease-in-out`。
- 效果参数：
  - 最低透明度：默认 `0.75`，范围 `0.3–0.95`，步长 `0.05`。
- 最高透明度固定为 `1`，不提供最高透明度参数。

### 9.4 光效脉冲

- 基础参数：
  - 循环时长：默认 `2s`，范围 `0.8–6s`，步长 `0.1s`。
  - 开始延迟：默认 `0s`，范围 `0–3s`。
  - 缓动曲线：默认 `ease-in-out`。
- 效果参数：
  - 光效颜色：未导入素材时默认品牌蓝；导入 SVG 后默认继承素材检测到的主色。
  - 光晕强度：默认 `50%`，范围 `0–100%`。
  - 光晕范围：默认 `18px`，范围 `0–48px`，步长 `1px`。
- 动画在无光效与设置的最大光效之间往返。
- 只有光效类动效显示光效参数。

### 9.5 慢速旋转

- 基础参数：
  - 循环时长：默认 `6s`，范围 `2–20s`，步长 `0.5s`。
  - 开始延迟：默认 `0s`，范围 `0–3s`，步长 `0.1s`。
- 缓动固定为 `linear`，参数面板不显示缓动曲线。
- 效果参数：
  - 旋转方向：顺时针、逆时针。
- 顺时针：`rotate(0deg) → rotate(360deg)`。
- 逆时针：`rotate(0deg) → rotate(-360deg)`。
- 旋转角度固定为完整一圈，不增加旋转角度、旋转中心或通用播放方向。
- 预览关键帧必须显式写出 `rotate(0deg)` 起点，避免浏览器把起终点矩阵都视为相同状态而不旋转。

## 10. 其他基础动效的专属参数原则

- 放大提示：只使用强调缩放、回弹缩放、时长、次数和缓动。
- 高亮发光：使用主色、基础光晕、峰值光晕、光效强度、时长、播放次数和缓动；光效强度范围 `0–100%`、默认 `70%`，播放次数为 `1–3 次`、默认 `1 次`。
- 边框高亮：深色内部保持稳定，只让边框亮度、内光和外光变化；使用边框颜色、边框宽度、基础光晕、峰值光晕、时长、播放次数和缓动；播放次数为 `1–3 次`、默认 `1 次`。
- 告警闪烁：使用告警颜色、最低透明度、闪烁频率和缓动；最高透明度固定为 `1`，参数面板不显示“最高透明度”。
- 脉冲扩散：使用主色、起始半径、扩散半径、扩散层数、层间间隔、扩散线宽和时长。
- 边框流光：
  - 流光沿矩形四边和圆角完整环绕，不能横向扫过卡片内部。
  - 使用流光颜色、流光长度、头尾透明度、头尾线宽、光晕、时长和方向。
  - 光头透明度约 `100%`，尾端衰减至 `0%`。
  - 光头较粗、尾端较细；当前实现约从 `3.4px` 过渡到 `0.5px`。
  - 头尾之间必须连续渐变，不能是生硬的等宽、等透明线段。
  - 主预览、缩略图和导出代码必须共用同一路径与动画逻辑。

## 11. SVG 导入规则

- 基础动效和装饰组件编辑页的入口统一命名为“导入 SVG”。
- 导入图标使用向下进入的图标，不使用方向相反的上传图标。
- 只允许 `.svg` / `image/svg+xml` 文件，大小不得超过 `2MB`。
- SVG 必须安全解析为内联 SVG，不能只通过 `<img>` 引用。
- 安全解析至少移除：
  - `script`
  - `style`
  - `link`
  - `foreignObject`
  - `iframe`
  - `object`
  - `embed`
  - 所有 `on*` 事件属性
  - `javascript:`、`expression()` 和不安全 `url()`
  - 非片段形式的外部 `href` / `xlink:href`
- 导入后移除 SVG 根节点固定宽高，让素材适应预览容器。
- 所有基础动效作用于素材外层容器，而不是绑定在默认蓝色矩形上。
- 未导入 SVG 时继续使用默认预览素材。
- 导入 SVG 后：
  - 只显示用户导入的 SVG。
  - 去掉原示例矩形、示例文字、背景、边框和阴影。
  - 当前动效继续作用于导入素材。
- SVG 样式属于素材状态，切换基础动效时保持不变，不能被动效默认值重置。
- 导入 SVG 后，右侧显示可折叠“SVG 样式”区域：
  - 颜色模式：保留原色、单色覆盖。
  - 填充颜色。
  - 描边颜色。
  - 描边宽度：`0–12px`，步长 `0.5px`。
  - 基础透明度：`0–1`，步长 `0.05`。
- 多色 SVG 默认使用“保留原色”。
- 只有选择“单色覆盖”后才显示统一填充色和描边色设置。
- 动效专属参数与 SVG 素材样式分开显示；不能让所有动效重复出现光效或颜色参数。
- 路径流光导入 SVG 时，从 `path、line、polyline、polygon、circle、ellipse、rect` 中读取可用路径元素。
- 导入 SVG 时记录素材的原始宽高与 `viewBox`；编辑器预览按可用画布等比适配，不改变素材比例。
- 复制代码和导出 HTML 使用素材原始尺寸，不把编辑器中的预览缩放写入交付文件。
- 导出 HTML 和复制代码必须包含：
  - 清理后的内联 SVG。
  - 当前 SVG 样式。
  - 当前动效代码。

## 12. 预览与播放规则

- 首页卡片使用悬停自动预览，不显示编辑器播放控制。
- 所有编辑画面统一使用紧凑播放控制，不再按短时或长时动效区分两套布局。
- 统一结构为：
  - `[重播图标 + 重播] [当前动效时长]`
- 控制区位于预览画面底部居中，高度约 `36px`，宽度随内容收缩。
- 时长只读，并与右侧当前动效时长或循环时长实时同步；保留实际精度，例如 `0.65s`。
- 点击“重播”从动画起点重新播放。
- 修改当前动效参数后自动重新播放一次。
- 循环动效在预览区域持续循环，切换离开后停止。
- 不显示：
  - 倍速。
  - 暂停或继续。
  - 进度条。
  - 当前时间 / 总时间。
  - 拖动进度。
  - 循环预览开关。
- 代码展示统一使用语法高亮，HTML、CSS、JSON 和 Vue 代码中的标签、属性、字符串、数值、关键字和注释应有颜色区分。

## 13. 保存、复制与导出规则

- 顶部“保存到我的动效”保存当前基础动效或装饰组件的可交付成果。
- 保存成果包含名称、独立 HTML 和预览 PNG；普通基础动效目前不承诺恢复完整编辑参数。
- 独立 HTML 使用中文页面标题、黑色页面背景，并将动效居中展示。
- “复制代码”复制当前 HTML + CSS。
- 基础动效与装饰组件的代码视图不显示重复的 HTML、Vue、JSON 格式切换。
- “导出 HTML”下载可独立打开的 HTML 文件。
- 导入 SVG 后，复制和导出结果必须包含当前 SVG、SVG 样式和动效。
- 编辑器像素格、编辑器按钮和参数面板不得进入导出 HTML。
- 装饰组件、路径流光和自定义 CSS 素材继续使用各自的专属生成器，但预览和导出必须保持一致。

## 14. 当前明确边界

- “我的动效”和自定义素材是浏览器本地数据，不支持账号同步、跨设备同步或团队共享。
- 清理浏览器站点数据可能丢失本地保存内容。
- 基础动效参数记忆只在当前打开会话内有效。
- “我的动效”没有自动进入项目交付流程。
- “项目交付”当前不属于可见产品流程，不得依据旧截图自行恢复。
- 本地 Git 提交、GitHub 代码和 Netlify 在线版本是三个不同状态；修改本地代码不会自动提交或上线。
- 若未来要改变以上边界，必须先与用户讨论并获得确认。
